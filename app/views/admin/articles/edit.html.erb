<% @page_title = "ニュース記事の編集" %>
<h1><%= @page_title %></h1>

<%= link_to("ニュース一覧に戻る", :admin_articles )%>
<%= link_to("記事の詳細に戻る", [:admin, @article] )%>

<%= form_for [:admin, @article] do |form| %>
  <%= render("form", form: form)%>
  <div>
    <%= form.submit %>
  </div>
<% end %>
